routes:
  - id: "r-warning-only"
    match:
      kind: prefix
      prefix: /api/v1/
    mutation: disabled
    compression:
      enabled: true
      min_size_bytes: 1024
      eligible_content_types:
        - "application/json"
      preferred_algorithms:
        - "gzip"
