routes:
  - id: "cache-vary-medium"
    match:
      kind: prefix
      prefix: /small-json
    mutation: full
    cache:
      enabled: true
      behavior: store
      ttl_seconds: 3600
      layers:
        l1:
          enabled: true
      vary_headers:
        - accept-language

