{{- if eq .Values.policy.mode "inline" -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bytetaper.fullname" . }}-policy
  labels:
    {{- include "bytetaper.labels" . | nindent 4 }}
data:
  policy.yaml: |
    {{- .Values.policy.inline | nindent 4 }}
{{- end -}}
