image:
  repository: ghcr.io/ByteTaper/bytetaper-runtime
  tag: ci

policy:
  mode: inline
  inline: |
    routes: []

l2Cache:
  enabled: true
  persistence:
    enabled: false
  emptyDir:
    enabled: true
